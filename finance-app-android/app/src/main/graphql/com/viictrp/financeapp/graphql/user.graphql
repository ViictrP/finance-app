mutation SaveUserTransaction($request: NewTransactionDTO!) {
    saveTransaction(newTransaction: $request) {
        id
        description
        amount
        type
        date
        category
    }
}

mutation DeleteTransaction($id: Int!, $all: Boolean) {
    deleteTransaction(id: $id, all: $all)
}